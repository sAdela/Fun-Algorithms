#include <iostream>
#include <string>
using namespace std;

int FirstFactorial(int num) {
	if (num == 0) return 1;

	if (num == 1)
		return num;

	return num * FirstFactorial(num - 1);

}

void main() {
	int num;
	cin >> num;
	cout << FirstFactorial(num);
	system("pause>0");
}
